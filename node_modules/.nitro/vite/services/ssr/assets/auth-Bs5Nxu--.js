import { t as supabase } from "./client-CFjc3-zE.js";
import { n as useAuth } from "./useAuth-DN8Ac26a.js";
import { t as Route } from "./auth-DZJYv_Ur.js";
import { n as cn, t as Button } from "./button-B2LyfGb_.js";
import { a as CardTitle, i as CardHeader, n as CardContent, r as CardDescription, t as Card } from "./card-DXaOETbi.js";
import { n as Input, t as Label } from "./label-sQVcd-s9.js";
import * as React from "react";
import { useEffect, useState } from "react";
import { Link, useNavigate } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
import { Activity, Loader2 } from "lucide-react";
import * as TabsPrimitive from "@radix-ui/react-tabs";
//#region src/components/ui/tabs.tsx
var Tabs = TabsPrimitive.Root;
var TabsList = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx(TabsPrimitive.List, {
	ref,
	className: cn("inline-flex h-9 items-center justify-center rounded-lg bg-muted p-1 text-muted-foreground", className),
	...props
}));
TabsList.displayName = TabsPrimitive.List.displayName;
var TabsTrigger = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx(TabsPrimitive.Trigger, {
	ref,
	className: cn("inline-flex items-center justify-center whitespace-nowrap rounded-md px-3 py-1 text-sm font-medium ring-offset-background cursor-pointer transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 disabled:cursor-not-allowed data-[state=active]:bg-background data-[state=active]:text-foreground data-[state=active]:shadow", className),
	...props
}));
TabsTrigger.displayName = TabsPrimitive.Trigger.displayName;
var TabsContent = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx(TabsPrimitive.Content, {
	ref,
	className: cn("mt-2 ring-offset-background focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2", className),
	...props
}));
TabsContent.displayName = TabsPrimitive.Content.displayName;
//#endregion
//#region src/routes/auth.tsx?tsr-split=component
function AuthPage() {
	const { mode } = Route.useSearch();
	const navigate = useNavigate();
	const { session, loading } = useAuth();
	useEffect(() => {
		if (!loading && session) navigate({ to: "/dashboard" });
	}, [
		loading,
		session,
		navigate
	]);
	return /* @__PURE__ */ jsx("div", {
		className: "flex min-h-screen items-center justify-center bg-soft-gradient px-5 py-12",
		children: /* @__PURE__ */ jsxs("div", {
			className: "w-full max-w-md",
			children: [/* @__PURE__ */ jsxs(Link, {
				to: "/",
				className: "mb-6 flex items-center justify-center gap-2 font-display text-lg font-semibold",
				children: [/* @__PURE__ */ jsx("span", {
					className: "grid size-9 place-items-center rounded-xl bg-hero-gradient text-primary-foreground",
					children: /* @__PURE__ */ jsx(Activity, { className: "size-5" })
				}), "Medisafe"]
			}), /* @__PURE__ */ jsx(Card, {
				className: "shadow-float",
				children: mode === "forgot" ? /* @__PURE__ */ jsx(ForgotPassword, {}) : /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsxs(CardHeader, { children: [/* @__PURE__ */ jsx(CardTitle, { children: "Welcome" }), /* @__PURE__ */ jsx(CardDescription, { children: "Sign in or create your Medisafe account." })] }), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsxs(Tabs, {
					defaultValue: mode === "register" ? "register" : "login",
					children: [
						/* @__PURE__ */ jsxs(TabsList, {
							className: "grid w-full grid-cols-2",
							children: [/* @__PURE__ */ jsx(TabsTrigger, {
								value: "login",
								children: "Log in"
							}), /* @__PURE__ */ jsx(TabsTrigger, {
								value: "register",
								children: "Register"
							})]
						}),
						/* @__PURE__ */ jsx(TabsContent, {
							value: "login",
							className: "pt-4",
							children: /* @__PURE__ */ jsx(LoginForm, {})
						}),
						/* @__PURE__ */ jsx(TabsContent, {
							value: "register",
							className: "pt-4",
							children: /* @__PURE__ */ jsx(RegisterForm, {})
						})
					]
				}) })] })
			})]
		})
	});
}
function LoginForm() {
	const navigate = useNavigate();
	const [busy, setBusy] = useState(false);
	async function onSubmit(event) {
		event.preventDefault();
		const form = new FormData(event.currentTarget);
		setBusy(true);
		const { error } = await supabase.auth.signInWithPassword({
			email: String(form.get("email")),
			password: String(form.get("password"))
		});
		setBusy(false);
		if (error) {
			toast.error(error.message === "Invalid login credentials" ? "That email or password is incorrect." : error.message);
			return;
		}
		toast.success("Signed in.");
		navigate({ to: "/dashboard" });
	}
	return /* @__PURE__ */ jsxs("form", {
		onSubmit,
		className: "space-y-4",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "space-y-2",
				children: [/* @__PURE__ */ jsx(Label, {
					htmlFor: "login-email",
					children: "Email"
				}), /* @__PURE__ */ jsx(Input, {
					id: "login-email",
					name: "email",
					type: "email",
					required: true,
					autoComplete: "email"
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "space-y-2",
				children: [/* @__PURE__ */ jsx(Label, {
					htmlFor: "login-password",
					children: "Password"
				}), /* @__PURE__ */ jsx(Input, {
					id: "login-password",
					name: "password",
					type: "password",
					required: true,
					autoComplete: "current-password"
				})]
			}),
			/* @__PURE__ */ jsxs(Button, {
				type: "submit",
				className: "w-full",
				disabled: busy,
				children: [busy && /* @__PURE__ */ jsx(Loader2, { className: "mr-2 size-4 animate-spin" }), " Log in"]
			}),
			/* @__PURE__ */ jsx(Link, {
				to: "/auth",
				search: { mode: "forgot" },
				className: "block text-center text-xs text-muted-foreground underline",
				children: "Forgot your password?"
			})
		]
	});
}
function RegisterForm() {
	const navigate = useNavigate();
	const [busy, setBusy] = useState(false);
	async function onSubmit(event) {
		event.preventDefault();
		const form = new FormData(event.currentTarget);
		const password = String(form.get("password"));
		if (password.length < 8) {
			toast.error("Use a password of at least 8 characters.");
			return;
		}
		setBusy(true);
		const { data, error } = await supabase.auth.signUp({
			email: String(form.get("email")),
			password,
			options: {
				emailRedirectTo: window.location.origin,
				data: {
					full_name: String(form.get("full_name")),
					date_of_birth: String(form.get("date_of_birth") ?? ""),
					gender: String(form.get("gender") ?? ""),
					phone: String(form.get("phone") ?? "")
				}
			}
		});
		setBusy(false);
		if (error) {
			toast.error(error.message);
			return;
		}
		if (data.session) {
			toast.success("Account created.");
			navigate({ to: "/dashboard" });
		} else toast.success("Check your email to confirm your account.");
	}
	return /* @__PURE__ */ jsxs("form", {
		onSubmit,
		className: "space-y-4",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "space-y-2",
				children: [/* @__PURE__ */ jsx(Label, {
					htmlFor: "reg-name",
					children: "Full name"
				}), /* @__PURE__ */ jsx(Input, {
					id: "reg-name",
					name: "full_name",
					required: true,
					autoComplete: "name"
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "space-y-2",
				children: [/* @__PURE__ */ jsx(Label, {
					htmlFor: "reg-email",
					children: "Email"
				}), /* @__PURE__ */ jsx(Input, {
					id: "reg-email",
					name: "email",
					type: "email",
					required: true,
					autoComplete: "email"
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "space-y-2",
				children: [/* @__PURE__ */ jsx(Label, {
					htmlFor: "reg-password",
					children: "Password"
				}), /* @__PURE__ */ jsx(Input, {
					id: "reg-password",
					name: "password",
					type: "password",
					required: true,
					minLength: 8,
					autoComplete: "new-password"
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid grid-cols-2 gap-3",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "space-y-2",
					children: [/* @__PURE__ */ jsx(Label, {
						htmlFor: "reg-dob",
						children: "Date of birth"
					}), /* @__PURE__ */ jsx(Input, {
						id: "reg-dob",
						name: "date_of_birth",
						type: "date",
						required: true
					})]
				}), /* @__PURE__ */ jsxs("div", {
					className: "space-y-2",
					children: [/* @__PURE__ */ jsx(Label, {
						htmlFor: "reg-phone",
						children: "Phone (optional)"
					}), /* @__PURE__ */ jsx(Input, {
						id: "reg-phone",
						name: "phone",
						type: "tel",
						autoComplete: "tel"
					})]
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "space-y-2",
				children: [/* @__PURE__ */ jsx(Label, {
					htmlFor: "reg-gender",
					children: "Gender (optional)"
				}), /* @__PURE__ */ jsx(Input, {
					id: "reg-gender",
					name: "gender"
				})]
			}),
			/* @__PURE__ */ jsxs(Button, {
				type: "submit",
				className: "w-full",
				disabled: busy,
				children: [busy && /* @__PURE__ */ jsx(Loader2, { className: "mr-2 size-4 animate-spin" }), " Create account"]
			})
		]
	});
}
function ForgotPassword() {
	const [busy, setBusy] = useState(false);
	const [sent, setSent] = useState(false);
	async function onSubmit(event) {
		event.preventDefault();
		const form = new FormData(event.currentTarget);
		setBusy(true);
		const { error } = await supabase.auth.resetPasswordForEmail(String(form.get("email")), { redirectTo: `${window.location.origin}/reset-password` });
		setBusy(false);
		if (error) {
			toast.error(error.message);
			return;
		}
		setSent(true);
	}
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsxs(CardHeader, { children: [/* @__PURE__ */ jsx(CardTitle, { children: "Reset your password" }), /* @__PURE__ */ jsx(CardDescription, { children: "We'll email you a link to choose a new password." })] }), /* @__PURE__ */ jsxs(CardContent, { children: [sent ? /* @__PURE__ */ jsx("p", {
		className: "text-sm text-muted-foreground",
		children: "If an account exists for that address, a reset link is on its way."
	}) : /* @__PURE__ */ jsxs("form", {
		onSubmit,
		className: "space-y-4",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "space-y-2",
			children: [/* @__PURE__ */ jsx(Label, {
				htmlFor: "forgot-email",
				children: "Email"
			}), /* @__PURE__ */ jsx(Input, {
				id: "forgot-email",
				name: "email",
				type: "email",
				required: true
			})]
		}), /* @__PURE__ */ jsxs(Button, {
			type: "submit",
			className: "w-full",
			disabled: busy,
			children: [busy && /* @__PURE__ */ jsx(Loader2, { className: "mr-2 size-4 animate-spin" }), " Send reset link"]
		})]
	}), /* @__PURE__ */ jsx(Link, {
		to: "/auth",
		search: { mode: "login" },
		className: "mt-4 block text-center text-xs text-muted-foreground underline",
		children: "Back to login"
	})] })] });
}
//#endregion
export { AuthPage as component };
