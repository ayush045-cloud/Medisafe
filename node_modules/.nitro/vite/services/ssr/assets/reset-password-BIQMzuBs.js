import { t as Button } from "./button-B2LyfGb_.js";
import { a as CardTitle, i as CardHeader, n as CardContent, r as CardDescription, t as Card } from "./card-DXaOETbi.js";
import { n as Input, t as Label } from "./label-sQVcd-s9.js";
import { t as changePassword } from "./profileService-M4Mi8wsm.js";
import { useState } from "react";
import { useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
import { Loader2 } from "lucide-react";
//#region src/routes/reset-password.tsx?tsr-split=component
function ResetPassword() {
	const navigate = useNavigate();
	const [busy, setBusy] = useState(false);
	async function onSubmit(event) {
		event.preventDefault();
		const password = String(new FormData(event.currentTarget).get("password"));
		if (password.length < 8) {
			toast.error("Use a password of at least 8 characters.");
			return;
		}
		setBusy(true);
		try {
			await changePassword(password);
			toast.success("Password updated.");
			navigate({ to: "/dashboard" });
		} catch (error) {
			toast.error(error instanceof Error ? error.message : "Something went wrong.");
		} finally {
			setBusy(false);
		}
	}
	return /* @__PURE__ */ jsx("div", {
		className: "flex min-h-screen items-center justify-center bg-soft-gradient px-5",
		children: /* @__PURE__ */ jsxs(Card, {
			className: "w-full max-w-md shadow-float",
			children: [/* @__PURE__ */ jsxs(CardHeader, { children: [/* @__PURE__ */ jsx(CardTitle, { children: "Choose a new password" }), /* @__PURE__ */ jsx(CardDescription, { children: "Open this page from the reset link in your email." })] }), /* @__PURE__ */ jsx(CardContent, { children: /* @__PURE__ */ jsxs("form", {
				onSubmit,
				className: "space-y-4",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "space-y-2",
					children: [/* @__PURE__ */ jsx(Label, {
						htmlFor: "new-password",
						children: "New password"
					}), /* @__PURE__ */ jsx(Input, {
						id: "new-password",
						name: "password",
						type: "password",
						required: true,
						minLength: 8
					})]
				}), /* @__PURE__ */ jsxs(Button, {
					type: "submit",
					className: "w-full",
					disabled: busy,
					children: [busy && /* @__PURE__ */ jsx(Loader2, { className: "mr-2 size-4 animate-spin" }), " Update password"]
				})]
			}) })]
		})
	});
}
//#endregion
export { ResetPassword as component };
