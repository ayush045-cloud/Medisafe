import { t as Button } from "./button-B2LyfGb_.js";
import { t as Badge } from "./badge-DZWKOgwu.js";
import { n as PageHeader, r as StateBlock } from "./AppShell-DyZ1QT5c.js";
import { n as CardContent, t as Card } from "./card-DXaOETbi.js";
import { i as markAlertRead, n as deleteAlert, r as getAlerts } from "./alertService-CowUMJia.js";
import { useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { Check, Loader2, Trash2 } from "lucide-react";
import { format } from "date-fns";
//#region src/routes/_authenticated/alerts.tsx?tsr-split=component
var FILTERS = [
	"all",
	"unread",
	"warning",
	"critical"
];
function AlertsPage() {
	const queryClient = useQueryClient();
	const [filter, setFilter] = useState("all");
	const alerts = useQuery({
		queryKey: ["alerts"],
		queryFn: getAlerts
	});
	const read = useMutation({
		mutationFn: ({ id, value }) => markAlertRead(id, value),
		onSuccess: () => queryClient.invalidateQueries({ queryKey: ["alerts"] }),
		onError: (e) => toast.error(e instanceof Error ? e.message : "Something went wrong.")
	});
	const remove = useMutation({
		mutationFn: deleteAlert,
		onSuccess: () => {
			queryClient.invalidateQueries({ queryKey: ["alerts"] });
			toast.success("Alert deleted.");
		},
		onError: (e) => toast.error(e instanceof Error ? e.message : "Something went wrong.")
	});
	const rows = (alerts.data ?? []).filter((a) => filter === "all" ? true : filter === "unread" ? !a.is_read : a.severity === filter);
	return /* @__PURE__ */ jsxs(Fragment, { children: [
		/* @__PURE__ */ jsx(PageHeader, {
			title: "Alert centre",
			description: "Notices created from your own medication and health records."
		}),
		/* @__PURE__ */ jsx("div", {
			className: "mb-4 flex flex-wrap gap-2",
			children: FILTERS.map((value) => /* @__PURE__ */ jsx(Button, {
				size: "sm",
				variant: filter === value ? "default" : "outline",
				onClick: () => setFilter(value),
				children: value[0].toUpperCase() + value.slice(1)
			}, value))
		}),
		alerts.isLoading ? /* @__PURE__ */ jsxs(StateBlock, { children: [/* @__PURE__ */ jsx(Loader2, { className: "mx-auto mb-2 size-4 animate-spin" }), "Loading alerts…"] }) : alerts.isError ? /* @__PURE__ */ jsx(StateBlock, { children: "Unable to load alerts. Please try again." }) : rows.length === 0 ? /* @__PURE__ */ jsx(StateBlock, { children: "No alerts to show." }) : /* @__PURE__ */ jsx("div", {
			className: "space-y-3",
			children: rows.map((alert) => /* @__PURE__ */ jsx(Card, {
				className: alert.is_read ? "opacity-70" : "shadow-card",
				children: /* @__PURE__ */ jsxs(CardContent, {
					className: "flex flex-wrap items-start justify-between gap-3 p-5",
					children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsxs("div", {
						className: "flex flex-wrap items-center gap-2",
						children: [
							/* @__PURE__ */ jsx(Badge, {
								className: alert.severity === "critical" ? "bg-destructive text-destructive-foreground" : alert.severity === "warning" ? "bg-warning text-warning-foreground" : "bg-accent text-accent-foreground",
								children: alert.severity
							}),
							/* @__PURE__ */ jsx("span", {
								className: "text-xs text-muted-foreground",
								children: alert.alert_type.replace(/_/g, " ")
							}),
							/* @__PURE__ */ jsx("span", {
								className: "text-xs text-muted-foreground",
								children: format(new Date(alert.created_at), "d MMM yyyy HH:mm")
							})
						]
					}), /* @__PURE__ */ jsx("p", {
						className: "mt-2 text-sm",
						children: alert.message
					})] }), /* @__PURE__ */ jsxs("div", {
						className: "flex gap-1",
						children: [/* @__PURE__ */ jsx(Button, {
							variant: "ghost",
							size: "icon",
							"aria-label": "Toggle read",
							onClick: () => read.mutate({
								id: alert.id,
								value: !alert.is_read
							}),
							children: /* @__PURE__ */ jsx(Check, { className: alert.is_read ? "size-4 text-muted-foreground" : "size-4 text-primary" })
						}), /* @__PURE__ */ jsx(Button, {
							variant: "ghost",
							size: "icon",
							"aria-label": "Delete alert",
							onClick: () => remove.mutate(alert.id),
							children: /* @__PURE__ */ jsx(Trash2, { className: "size-4 text-destructive" })
						})]
					})]
				})
			}, alert.id))
		})
	] });
}
//#endregion
export { AlertsPage as component };
