import { n as cn, t as Button } from "./button-B2LyfGb_.js";
import { n as PageHeader, r as StateBlock } from "./AppShell-DyZ1QT5c.js";
import { a as CardTitle, i as CardHeader, n as CardContent, t as Card } from "./card-DXaOETbi.js";
import { t as createAlert } from "./alertService-CowUMJia.js";
import { a as getVitals, i as deleteVital, n as checkVitalRanges, r as createVital, t as REFERENCE_RANGES } from "./vitalService-wqC1N4RR.js";
import { n as Input, t as Label } from "./label-sQVcd-s9.js";
import { a as DialogTitle, i as DialogHeader, n as DialogContent, o as Textarea, r as DialogDescription, t as Dialog } from "./dialog-Csm8sY-W.js";
import * as React from "react";
import { useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { Loader2, Plus, Trash2 } from "lucide-react";
import { format } from "date-fns";
import { CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
//#region src/components/ui/table.tsx
var Table = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx("div", {
	className: "relative w-full overflow-auto",
	children: /* @__PURE__ */ jsx("table", {
		ref,
		className: cn("w-full caption-bottom text-sm", className),
		...props
	})
}));
Table.displayName = "Table";
var TableHeader = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx("thead", {
	ref,
	className: cn("[&_tr]:border-b", className),
	...props
}));
TableHeader.displayName = "TableHeader";
var TableBody = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx("tbody", {
	ref,
	className: cn("[&_tr:last-child]:border-0", className),
	...props
}));
TableBody.displayName = "TableBody";
var TableFooter = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx("tfoot", {
	ref,
	className: cn("border-t bg-muted/50 font-medium [&>tr]:last:border-b-0", className),
	...props
}));
TableFooter.displayName = "TableFooter";
var TableRow = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx("tr", {
	ref,
	className: cn("border-b transition-colors hover:bg-muted/50 data-[state=selected]:bg-muted", className),
	...props
}));
TableRow.displayName = "TableRow";
var TableHead = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx("th", {
	ref,
	className: cn("h-10 px-2 text-left align-middle font-medium text-muted-foreground [&:has([role=checkbox])]:pr-0 [&>[role=checkbox]]:translate-y-[2px]", className),
	...props
}));
TableHead.displayName = "TableHead";
var TableCell = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx("td", {
	ref,
	className: cn("p-2 align-middle [&:has([role=checkbox])]:pr-0 [&>[role=checkbox]]:translate-y-[2px]", className),
	...props
}));
TableCell.displayName = "TableCell";
var TableCaption = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsx("caption", {
	ref,
	className: cn("mt-4 text-sm text-muted-foreground", className),
	...props
}));
TableCaption.displayName = "TableCaption";
//#endregion
//#region src/routes/_authenticated/health.tsx?tsr-split=component
var METRICS = [
	{
		key: "blood_pressure_systolic",
		label: "Systolic BP",
		unit: "mmHg"
	},
	{
		key: "blood_pressure_diastolic",
		label: "Diastolic BP",
		unit: "mmHg"
	},
	{
		key: "heart_rate",
		label: "Heart rate",
		unit: "bpm"
	},
	{
		key: "blood_glucose",
		label: "Blood glucose",
		unit: "mmol/L"
	},
	{
		key: "temperature",
		label: "Temperature",
		unit: "°C"
	},
	{
		key: "oxygen_saturation",
		label: "Oxygen saturation",
		unit: "%"
	},
	{
		key: "weight",
		label: "Weight",
		unit: "kg"
	}
];
function HealthPage() {
	const queryClient = useQueryClient();
	const [open, setOpen] = useState(false);
	const [metric, setMetric] = useState("blood_pressure_systolic");
	const vitals = useQuery({
		queryKey: ["vitals"],
		queryFn: () => getVitals(200)
	});
	const remove = useMutation({
		mutationFn: deleteVital,
		onSuccess: () => {
			queryClient.invalidateQueries({ queryKey: ["vitals"] });
			toast.success("Record deleted.");
		},
		onError: (e) => toast.error(e instanceof Error ? e.message : "Something went wrong.")
	});
	const rows = vitals.data ?? [];
	const series = rows.slice().reverse().filter((row) => row[metric] !== null).map((row) => ({
		date: format(new Date(row.recorded_at), "d MMM HH:mm"),
		value: Number(row[metric])
	}));
	return /* @__PURE__ */ jsxs(Fragment, { children: [
		/* @__PURE__ */ jsx(PageHeader, {
			title: "Health logs",
			description: "Vitals you have recorded, with informational reference ranges.",
			action: /* @__PURE__ */ jsxs(Button, {
				onClick: () => setOpen(true),
				children: [/* @__PURE__ */ jsx(Plus, { className: "mr-2 size-4" }), " Add reading"]
			})
		}),
		/* @__PURE__ */ jsxs(Card, {
			className: "mb-6",
			children: [/* @__PURE__ */ jsxs(CardHeader, {
				className: "flex-row items-center justify-between gap-3 space-y-0",
				children: [/* @__PURE__ */ jsx(CardTitle, {
					className: "text-base",
					children: "Trend"
				}), /* @__PURE__ */ jsx("select", {
					"aria-label": "Metric",
					value: metric,
					onChange: (event) => setMetric(event.target.value),
					className: "h-9 rounded-md border border-input bg-transparent px-3 text-sm",
					children: METRICS.map((m) => /* @__PURE__ */ jsx("option", {
						value: m.key,
						children: m.label
					}, m.key))
				})]
			}), /* @__PURE__ */ jsx(CardContent, { children: vitals.isLoading ? /* @__PURE__ */ jsxs(StateBlock, { children: [/* @__PURE__ */ jsx(Loader2, { className: "mx-auto mb-2 size-4 animate-spin" }), "Loading health data…"] }) : vitals.isError ? /* @__PURE__ */ jsx(StateBlock, { children: "Unable to load your health records. Please try again." }) : series.length === 0 ? /* @__PURE__ */ jsx(StateBlock, { children: "No health data available yet for this measurement." }) : /* @__PURE__ */ jsx("div", {
				className: "h-72",
				children: /* @__PURE__ */ jsx(ResponsiveContainer, {
					width: "100%",
					height: "100%",
					children: /* @__PURE__ */ jsxs(LineChart, {
						data: series,
						children: [
							/* @__PURE__ */ jsx(CartesianGrid, {
								strokeDasharray: "3 3",
								stroke: "var(--border)"
							}),
							/* @__PURE__ */ jsx(XAxis, {
								dataKey: "date",
								stroke: "var(--muted-foreground)",
								fontSize: 12
							}),
							/* @__PURE__ */ jsx(YAxis, {
								stroke: "var(--muted-foreground)",
								fontSize: 12,
								domain: ["auto", "auto"]
							}),
							/* @__PURE__ */ jsx(Tooltip, { contentStyle: {
								borderRadius: 12,
								border: "1px solid var(--border)",
								background: "var(--card)"
							} }),
							/* @__PURE__ */ jsx(Line, {
								type: "monotone",
								dataKey: "value",
								stroke: "var(--chart-1)",
								strokeWidth: 2,
								dot: true
							})
						]
					})
				})
			}) })]
		}),
		/* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, {
			className: "text-base",
			children: "History"
		}) }), /* @__PURE__ */ jsx(CardContent, {
			className: "overflow-x-auto",
			children: rows.length === 0 ? /* @__PURE__ */ jsx(StateBlock, { children: "No health readings recorded yet." }) : /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, { children: [
				/* @__PURE__ */ jsx(TableHead, { children: "Recorded" }),
				/* @__PURE__ */ jsx(TableHead, { children: "BP" }),
				/* @__PURE__ */ jsx(TableHead, { children: "HR" }),
				/* @__PURE__ */ jsx(TableHead, { children: "Glucose" }),
				/* @__PURE__ */ jsx(TableHead, { children: "Temp" }),
				/* @__PURE__ */ jsx(TableHead, { children: "SpO₂" }),
				/* @__PURE__ */ jsx(TableHead, { children: "Weight" }),
				/* @__PURE__ */ jsx(TableHead, {})
			] }) }), /* @__PURE__ */ jsx(TableBody, { children: rows.map((row) => /* @__PURE__ */ jsxs(TableRow, { children: [
				/* @__PURE__ */ jsx(TableCell, {
					className: "whitespace-nowrap",
					children: format(new Date(row.recorded_at), "d MMM yyyy HH:mm")
				}),
				/* @__PURE__ */ jsx(TableCell, { children: row.blood_pressure_systolic && row.blood_pressure_diastolic ? `${row.blood_pressure_systolic}/${row.blood_pressure_diastolic}` : "—" }),
				/* @__PURE__ */ jsx(TableCell, { children: row.heart_rate ?? "—" }),
				/* @__PURE__ */ jsx(TableCell, { children: row.blood_glucose ?? "—" }),
				/* @__PURE__ */ jsx(TableCell, { children: row.temperature ?? "—" }),
				/* @__PURE__ */ jsx(TableCell, { children: row.oxygen_saturation ?? "—" }),
				/* @__PURE__ */ jsx(TableCell, { children: row.weight ?? "—" }),
				/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsx(Button, {
					variant: "ghost",
					size: "icon",
					onClick: () => remove.mutate(row.id),
					"aria-label": "Delete reading",
					children: /* @__PURE__ */ jsx(Trash2, { className: "size-4 text-destructive" })
				}) })
			] }, row.id)) })] })
		})] }),
		/* @__PURE__ */ jsxs("p", {
			className: "mt-6 text-xs text-muted-foreground",
			children: [
				"Reference ranges shown are general adult informational ranges (",
				Object.values(REFERENCE_RANGES).map((r) => `${r.label} ${r.min}–${r.max} ${r.unit}`).join("; "),
				"). They are informational only and are not a diagnosis. Consult a qualified healthcare professional about your results."
			]
		}),
		/* @__PURE__ */ jsx(AddVitalDialog, {
			open,
			onOpenChange: setOpen
		})
	] });
}
function AddVitalDialog({ open, onOpenChange }) {
	const queryClient = useQueryClient();
	const [busy, setBusy] = useState(false);
	async function onSubmit(event) {
		event.preventDefault();
		const form = new FormData(event.currentTarget);
		const num = (key) => {
			const raw = String(form.get(key) ?? "").trim();
			return raw === "" ? null : Number(raw);
		};
		const payload = {
			recorded_at: new Date(String(form.get("recorded_at"))).toISOString(),
			blood_pressure_systolic: num("blood_pressure_systolic"),
			blood_pressure_diastolic: num("blood_pressure_diastolic"),
			heart_rate: num("heart_rate"),
			blood_glucose: num("blood_glucose"),
			temperature: num("temperature"),
			oxygen_saturation: num("oxygen_saturation"),
			weight: num("weight"),
			notes: String(form.get("notes") ?? "").trim() || null
		};
		if (Object.entries(payload).every(([key, value]) => key === "recorded_at" || value === null)) {
			toast.error("Enter at least one measurement.");
			return;
		}
		setBusy(true);
		try {
			const warnings = checkVitalRanges(await createVital(payload));
			for (const message of warnings) await createAlert({
				alert_type: "health_warning",
				severity: "warning",
				message
			});
			queryClient.invalidateQueries({ queryKey: ["vitals"] });
			queryClient.invalidateQueries({ queryKey: ["alerts"] });
			toast.success(warnings.length ? "Reading saved. Some values are outside the reference range." : "Reading saved successfully.");
			onOpenChange(false);
		} catch (error) {
			toast.error(error instanceof Error ? error.message : "Something went wrong.");
		} finally {
			setBusy(false);
		}
	}
	const nowLocal = (/* @__PURE__ */ new Date(Date.now() - (/* @__PURE__ */ new Date()).getTimezoneOffset() * 6e4)).toISOString().slice(0, 16);
	return /* @__PURE__ */ jsx(Dialog, {
		open,
		onOpenChange,
		children: /* @__PURE__ */ jsxs(DialogContent, {
			className: "max-h-[90vh] overflow-y-auto sm:max-w-lg",
			children: [/* @__PURE__ */ jsxs(DialogHeader, { children: [/* @__PURE__ */ jsx(DialogTitle, { children: "Add a health reading" }), /* @__PURE__ */ jsx(DialogDescription, { children: "Leave any measurement blank if you didn't take it." })] }), /* @__PURE__ */ jsxs("form", {
				onSubmit,
				className: "space-y-4",
				children: [
					/* @__PURE__ */ jsxs("div", {
						className: "space-y-2",
						children: [/* @__PURE__ */ jsx(Label, {
							htmlFor: "recorded_at",
							children: "Recorded at"
						}), /* @__PURE__ */ jsx(Input, {
							id: "recorded_at",
							name: "recorded_at",
							type: "datetime-local",
							defaultValue: nowLocal,
							required: true
						})]
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "grid grid-cols-2 gap-3",
						children: [
							/* @__PURE__ */ jsx(NumField, {
								label: "Systolic (mmHg)",
								name: "blood_pressure_systolic"
							}),
							/* @__PURE__ */ jsx(NumField, {
								label: "Diastolic (mmHg)",
								name: "blood_pressure_diastolic"
							}),
							/* @__PURE__ */ jsx(NumField, {
								label: "Heart rate (bpm)",
								name: "heart_rate"
							}),
							/* @__PURE__ */ jsx(NumField, {
								label: "Blood glucose (mmol/L)",
								name: "blood_glucose",
								step: "0.1"
							}),
							/* @__PURE__ */ jsx(NumField, {
								label: "Temperature (°C)",
								name: "temperature",
								step: "0.1"
							}),
							/* @__PURE__ */ jsx(NumField, {
								label: "Oxygen saturation (%)",
								name: "oxygen_saturation"
							}),
							/* @__PURE__ */ jsx(NumField, {
								label: "Weight (kg)",
								name: "weight",
								step: "0.1"
							})
						]
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "space-y-2",
						children: [/* @__PURE__ */ jsx(Label, {
							htmlFor: "notes",
							children: "Notes"
						}), /* @__PURE__ */ jsx(Textarea, {
							id: "notes",
							name: "notes",
							rows: 2
						})]
					}),
					/* @__PURE__ */ jsxs(Button, {
						type: "submit",
						className: "w-full",
						disabled: busy,
						children: [busy && /* @__PURE__ */ jsx(Loader2, { className: "mr-2 size-4 animate-spin" }), " Save reading"]
					})
				]
			})]
		})
	});
}
function NumField({ label, name, step }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-2",
		children: [/* @__PURE__ */ jsx(Label, {
			htmlFor: name,
			children: label
		}), /* @__PURE__ */ jsx(Input, {
			id: name,
			name,
			type: "number",
			step: step ?? "1",
			inputMode: "decimal"
		})]
	});
}
//#endregion
export { HealthPage as component };
